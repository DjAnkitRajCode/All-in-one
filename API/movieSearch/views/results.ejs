<h1>Movie Search page</h1>
<ul>
    <% data["Search"].forEach(function(movie){ %>
        <li><img src="<%= movie["Poster"] %>"><strong><%= movie["Title"] %></strong> -- <%= movie["Year"] %></li>
    <% }); %>
</ul>

<a href="/">Search Again!</a>